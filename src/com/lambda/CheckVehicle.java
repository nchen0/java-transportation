package com.lambda;

public interface CheckVehicle {
    // CheckVehicle will tell us that we will need to implement a method called test, taking an abstract vehicle type as a parameter;
    boolean test(AbstractVehicle v);
}
